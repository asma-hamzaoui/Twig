<!DOCTYPE html>
<html lang="fr">
<head>
    <title>Détails de {{ author.userName }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        h1 {
            text-align: center;
            color: #2196F3;
        }
        table {
            width: 50%;
            margin: 40px auto;
            border-collapse: collapse;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        th, td {
            padding: 12px 15px;
            text-align: center;
            border: 2px solid #ddd;
        }
        th {
            background-color: #2196F3;
            color: black;
        }
        tr:hover {
            background-color: #ddd;
        }
        img {
            border-radius: 8px;
            max-width: 100%;
            height: auto;
        }
    </style>
</head>
<body>

    <h1>Détails de {{ author.userName }}</h1>

    <table>
        <tr>
            <th>Image</th>
            <th>Nom</th>
            <th>Email</th>
            <th>Nombre de livres</th>
        </tr>
        <tr>
            <td>
                <img src="{{ asset(author.image) }}" alt="Image de {{ author.userName }}" width="100" height="100">
                <p>Chemin de l'image : {{ asset(author.image) }}</p> <!-- Vérification du chemin -->
            </td>
            <td>{{ author.userName | upper }}</td>
            <td>{{ author.email }}</td>
            <td>{{ author.nbBooks }}</td>
        </tr>
    </table>

</body>
</html>